services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pwsi
      POSTGRES_PASSWORD: pwsi
      POSTGRES_DB: pwsi
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
